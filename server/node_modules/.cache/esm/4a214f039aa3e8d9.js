let express,cors,morgan,mongoose,dotenv,userRouter;_9aa‍.w("express",[["default",["express"],function(v){express=v}]]);_9aa‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_9aa‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_9aa‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_9aa‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_9aa‍.w("./routes/user",[["default",["userRouter"],function(v){userRouter=v}]]);








dotenv.config();
const PORT = process.env.PORT || 5000;
const MONGODB_URL = process.env.MONGODB_URL;

const app = express();

app.use(morgan('dev'));
app.use(express.json({
    limit: "30mb",
    extended: true,
}));
app.use(express.urlencoded({
    limit: "30mb",
    extended: true,
}));
app.use(cors());

app.use("/users", userRouter);

mongoose.connect(MONGODB_URL)
    .then(() => {
        app.listen(PORT, () => {
            console.log(`Server running on port ${PORT}`);
        });
    })
    .catch(err => {
        console.log(`Server did not connect.\n${err} `);
    });